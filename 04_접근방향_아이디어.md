
```markdown
# 04. 접근 방향 & 아이디어 💡

> 목적: "무엇을 어떤 순서로 시도할지" 우선순위를 정한다.
> 원칙: (1) 누수 없음 (2) 10분 안에 추론 (3) 오프라인 재현 가능
> 전략: 단순→복잡. 매 단계 상수예측 Brier를 이기는지 확인.

---

## 0. 큰 그림 🗺️

```
[상수 베이스라인] → [단순 통계 모델] → [GBDT 단일] → [피처 강화] → [캘리브레이션] → [앙상블]
     기준선            빠른 하한선        주력 모델      점수 상승      확률 보정      마지막 짜내기
```

> 대부분의 점수는 **GBDT 단일 + 피처 강화 + 캘리브레이션**에서 나온다.
> 앙상블은 시간(10분) 여유 있을 때만.

---

## 1. 검증 전략 (가장 먼저 고정) 🔒

> **이게 틀리면 이후 모든 실험이 거짓말이 된다.**

### 1-1. 시즌 기반 split (권장)

EDA에서 확인한 **train(과거)→test(2025 미래)** 구조를 그대로 모방:

```python
# 예: 마지막 시즌을 검증셋으로 (holdout)
train_seasons = sorted(train["season"].unique())
valid_season  = train_seasons[-1]          # 가장 최근 시즌
tr = train[train["season"] < valid_season]
va = train[train["season"] == valid_season]
```
**이유:** 랜덤 KFold는 미래 정보가 과거 검증에 섞여 **점수를 부풀림**.
시즌 split은 실제 제출 상황(미래 예측)과 일치 → **CV-LB 갭이 작다**.

### 1-2. 시즌 그룹 KFold (데이터 여유 있으면)

```python
from sklearn.model_selection import GroupKFold
gkf = GroupKFold(n_splits=len(train_seasons)-1)  # 시즌을 그룹으로
# groups = train["season"]
```
> holdout보다 안정적이지만 학습 시간↑. 실험 초반엔 holdout으로 빠르게.

### 1-3. 평가지표 = Brier / BSS 로 직접 측정

```python
def brier(y, p):
    return np.mean((y - p) ** 2)

def bss(y, p, p_base):
    return 1 - brier(y, p) / brier(y, np.full_like(p, p_base))
# p_base = tr["control_success"].mean()  (train 기준 상수)
```
> 대회 지표를 로컬에서 그대로 계산 → LB 없이도 방향 판단.

---

## 2. 모델 선택 🤖

### 2-1. 주력: GBDT (LightGBM 우선)

**이유:**
- 테이블 데이터 + 결측/범주 혼재 → GBDT가 강함
- **결측을 그대로 처리** (asof 결측 fallback 부담↓)
- 147만행 × 47피처 → 학습 빠름, **추론 10분 여유**

```python
import lightgbm as lgb

params = {
    "objective": "binary",
    "metric": "binary_logloss",   # 확률 출력 학습
    "learning_rate": 0.03,
    "num_leaves": 63,
    "min_child_samples": 200,     # 과적합 억제(대용량이라 크게)
    "feature_fraction": 0.8,
    "bagging_fraction": 0.8,
    "bagging_freq": 1,
    "seed": 42,
    "verbose": -1,
}
```
> ⚠️ **objective는 binary(확률)**. Brier는 확률 예측을 평가하므로
> 회귀가 아니라 확률 출력을 그대로 쓴다.

**대안:** XGBoost, CatBoost(범주 많으면 유리).
→ 오프라인 설치 가능 여부부터 requirements.txt로 검증.

### 2-2. 하한선 확인용: 단순 통계 모델

```python
# pitcher별 과거 성공률 (asof 방식, 스무딩)
# global_p = tr["control_success"].mean()
# 스무딩: (성공수 + a*global_p) / (시도수 + a)
```
> GBDT가 이 단순 모델보다 확실히 나은지 확인 → 삽질 방지.

---

## 3. 피처 전략 🛠️

### 3-1. 기본 (제공 피처 활용)

- **asof_* 그대로 사용** (공식 제공 = 누수 안전한 과거 집계)
- 결측 처리:
  - GBDT는 NaN 그대로 학습 가능 → **1차는 대체 안 함**
  - **결측 플래그** 추가: `df["asof_x_isna"] = df["asof_x"].isna()`
    → cold-start(신인) 여부가 신호일 수 있음

### 3-2. 범주형 인코딩

```python
# 저카디널리티: category dtype (LGBM 네이티브)
for c in cat_cols:
    train[c] = train[c].astype("category")

# 고카디널리티(pitcher_id 등): target encoding (누수 주의!)
```
> ⚠️ **target encoding은 반드시 fold 내부에서** 계산 (검증 누수 방지).
> 또는 사전집계값을 `model/`에 저장 후 추론 시 매핑.

### 3-3. trackman 활용 (EDA 5번 결과에 따라 분기) ⭐

```
IF ID 교집합 높음 (같은 체계):
   → pitcher별 trackman 요약(평균 구속/회전/무브먼트 등)을
     asof 방식(현재 투구 이전 시즌까지)으로 집계 → 조인
   → 반드시 시즌 이전 데이터만 사용 (누수 금지)

ELIF ID 교집합 낮음/0 (다른 체계):
   → 직접 조인 불가
   → 구종군/투수유형 단위 통계로만 간접 활용
   → 또는 trackman 미사용 (제공 asof 피처가 이미 요약본일 수 있음)
```

**핵심 규칙 (01 문서):**
- 학습 데이터 **사전 집계만 허용** → trackman 통계는 `model/`에 저장
- 추론 시 **테스트 행 간 참조 금지** → 미리 만든 매핑 테이블만 lookup

### 3-4. 파생 피처 (EDA 근거 있을 때만)

- 카운트(볼-스트라이크) 조합, 이닝/점수차 구간화
- asof 피처 간 비율/차이 (예: 최근 vs 통산 성공률 차)
> ⚠️ **피처는 test에서도 만들 수 있어야 함** (train-only 컬럼 파생 금지).

---

## 4. 확률 캘리브레이션 (Brier 직결) 📊

> Brier는 **확률의 정확도**를 본다 → 캘리브레이션이 점수에 직접 기여.

```python
# 1) 신뢰도 곡선 점검
from sklearn.calibration import calibration_curve
prob_true, prob_pred = calibration_curve(va_y, va_pred, n_bins=10)
# 대각선에서 벗어나면 보정 필요
```

**보정 방법:**
- **Isotonic Regression** (데이터 많을 때 강력, 147만행 → 적합)
- **Platt Scaling**(로지스틱) — 단조·안정적
```python
from sklearn.isotonic import IsotonicRegression
iso = IsotonicRegression(out_of_bounds="clip")
iso.fit(va_pred, va_y)          # 검증셋으로 학습
final = iso.predict(test_pred)  # 테스트에 적용
```
> ⚠️ 캘리브레이터는 **검증셋으로 학습** → 학습셋 재사용 시 누수.

---

## 5. 앙상블 (시간 여유 있을 때) 🎯

```
- seed 앙상블: 같은 모델 seed만 바꿔 평균 (안정성↑, 저비용)
- 모델 다양성: LGBM + CatBoost 가중 평균
- 확률 평균은 산술평균 or 로짓 평균
```
> ⚠️ 10분 추론 제약 → **모델 수 × 추론시간** 반드시 계산.
> 무거운 앙상블로 시간 초과 = 0점.

---

## 6. 우선순위 로드맵 (실행 순서) 📋

| 순위 | 작업 | 목표 | 리스크 |
| --- | --- | --- | --- |
| 1 | 시즌 split + 상수 베이스라인 | 기준선 Brier 확정 | 없음 |
| 2 | LGBM 단일 (제공 피처만, 결측 그대로) | 첫 유효 점수 | 낮음 |
| 3 | 결측 플래그 + 범주 인코딩 | 소폭 상승 | 낮음 |
| 4 | 캘리브레이션(Isotonic) | Brier 직접 개선 ⭐ | 낮음 |
| 5 | trackman 조인(가능시) | 피처 강화 | **누수/시간** |
| 6 | 파생 피처 실험 | 추가 상승 | 과적합 |
| 7 | seed/모델 앙상블 | 마지막 짜내기 | **10분 초과** |

> 💡 **2~4번에서 대부분의 점수**가 나온다. 여기에 집중.

---

## 7. 하지 말아야 할 것 🚫

```
✗ 랜덤 KFold (미래 예측인데 시간 누수 → CV-LB 갭 폭발)
✗ test 통계로 피처/인코딩 만들기 (규칙 위반)
✗ 테스트 행 간 참조 (예: test 전체 평균으로 정규화)
✗ 학습셋으로 캘리브레이터 학습 (누수)
✗ 검증 없이 LB만 보고 튜닝 (제출 5회/일 제약 + 과적합)
✗ 오프라인 미검증 패키지 사용 (설치 실패 = 0점)
```

---

## 8. 실험 → 문서 연결

| 이 문서의 결정 | 기록 위치 |
| --- | --- |
| 검증 split 방식 | 05 실험로그 (고정) |
| 각 실험 Brier/BSS | 05 실험로그 |
| 최종 파이프라인 | 06 제출체크리스트 |
| 누수 주의 항목 | 01 제약과금지사항 |

---

## 한 줄 요약

> **"시즌 split으로 정직하게 검증하고, GBDT+캘리브레이션에 집중한다.
> trackman과 앙상블은 누수·시간 검증을 통과할 때만."**
```

---

## 작성 완료 ✅

**이 문서의 설계 포인트:**

- 🔒 **1번 검증 전략을 최우선 고정** — 시즌 split이 아니면 이후 실험이 전부 거짓말이 되는 점 강조
- 📊 **4번 캘리브레이션을 핵심으로 배치** — Brier 지표는 확률 정확도를 보므로 여


---

## 7. 최종 대회 결과 및 아이디어 채택/기각 총결산

> **최종 점수**: `1121.9039933605` 점 (Public 180위)  
> **최종 제출**: `submit_ref4_super113A.zip`

### 1. 최종 채택된 핵심 아이디어 (Adopted)
1. **3-Tier Multi-Family GBDT Super Ensemble**: CatBoost(Base/Futures) + LightGBM(Regular) + XGBoost의 이종 모델 결합.
2. **Zero-Centered Adaptive Hierarchical Meta Gate**: 상황 압박(`li`, 볼카운트)에 따른 비선형 잔차 보정.
3. **1군/2군 Macro-Leap Decoupling**: 정규리그와 퓨처스리그의 리그별 특성 분리 모델링 (2군 OOF +3172.81pt 폭증).
4. **Disjoint Matchup Empirical Bayes (113A Engine)**: $K=300$ 수축 기반 상대전적 정규화 결합 ($w=0.035$).
5. **Strict Temporal Forward Split**: 미래 시즌 예측을 위한 엄격한 $T_{\text{train}} < T_{\text{val}}$ 검증 체계.

### 2. 검증 후 최종 기각된 아이디어 (Rejected / Zombie Components)
1. **과도한 Adaptive Gate Scale (0.75)**: 예측 분산 +130% 폭증으로 Brier 감점 유발 $\rightarrow$ $0.05$로 보수적 축소.
2. **Random K-Fold OOF Stacking**: 시계열 누수로 인한 일반화 붕괴 확인 $\rightarrow$ 영구 금지.
3. **전역 Variance Shrinkage (alpha=0.96)**: 테스트셋 고신뢰도 분별력 압축 $\rightarrow$ alpha=1.0 원형 복원.
4. **v66 Nested Deviations / 미보정 Bradley-Terry**: 2025 평가셋 왜곡 유발로 117 감사에서 공식 기각.
