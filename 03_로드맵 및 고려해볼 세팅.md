
```markdown
# 03. 로드맵 및 고려해볼 세팅 🗺️

> 목표: Brier Skill Score **549.51점(수료) 이상** 안정적 달성 → 이후 상위권 도전.
> 원칙: "사전 집계는 과거에서만, 추론은 한 행씩 독립적으로." (→ 01번 문서)

---

## Part A. 전체 로드맵

### 단계 개요
```
[0] 환경 세팅        → 재현 가능한 오프라인 호환 환경 구축
[1] EDA             → 데이터 이해 + ID 매핑 + 누수 점검
[2] 검증 설계        → 시즌 기반 split (미래 예측 구조 모사)
[3] 베이스라인 재현   → 제출 파이프라인 end-to-end 확인
[4] 피처 엔지니어링   → asof 활용 + trackman 사전집계
[5] 모델링/캘리브레이션 → GBDT + 확률 보정
[6] 검증·튜닝        → CV 신뢰도 확보, 과적합 관리
[7] 제출 파이프라인   → script.py 일관성 + 시간/용량 점검
[8] 반복 개선        → 1일 5회 제한 내 효율적 실험
```

---

### [0] 환경 세팅
- 로컬(학습)과 제출환경(추론)의 **패키지 버전 일치** 확보.
- 오프라인 실행 가능하도록 의존성 최소화.
- (상세는 Part B 참조)

### [1] EDA — `02_데이터탐색_EDA.md`
**필수 확인 항목:**
- `control_success` 클래스 비율 (불균형 여부)
- `season` 분포: train은 어디까지? test(2025)와의 gap
- `asof_*` 결측 비율 (cold-start 규모)
- **ID 매핑 검증**: `pitcher_id` ↔ `pitcher_trackman_id` 조인 가능한가?
  - 불가하면 trackman은 "구종군/투수유형 단위 요약"으로만 활용
- 누수 의심 컬럼 유무 재점검

### [2] 검증 설계 (가장 중요) 🎯
> 대회 구조 = **학습(2019~2024) → 평가(2025)**. 미래 예측.

- **시즌 기반 holdout / GroupKFold(by season)** 권장.
  - 예: 마지막 시즌을 검증셋으로 → "미래 예측" 상황 모사
- 랜덤 K-Fold는 **낙관적 편향** 위험 (시간 누수).
- 사전집계 피처는 **fold 내 train 부분에서만 계산** → valid에 map (누수 방지).

### [3] 베이스라인 재현
- 제공 baseline 코드로 **제출 → 리더보드 0점 근처 확인**.
- **end-to-end 파이프라인 먼저 완성** (성능보다 "돌아가는 것" 우선).
- 이 시점에 `script.py` 입출력 경로/양식 확정.

### [4] 피처 엔지니어링 — `04_접근방향.md`
- `asof_*` 그대로 사용 + 파생 (비율 차이, 최근성 등)
- 카운트 상황 파생: `balls-strikes` 조합, 압박 상황 플래그
- trackman 사전집계 (조인 가능 시): 투수별 평균 구속/무브먼트 등
- **모든 사전집계는 train/trackman에서만** → dict/parquet로 `model/`에 저장

### [5] 모델링 + 캘리브레이션
- 1순위: **LightGBM / XGBoost** (표 데이터 강력, 빠름)
- 평가지표가 **Brier 계열** → **확률 캘리브레이션 필수**
  - `objective=binary`, 예측확률 그대로 or Isotonic/Platt 보정
- 딥러닝은 우선순위 낮음 (표 데이터에서 GBDT가 보통 우세 + 10분 제약)

### [6] 검증·튜닝
- CV-리더보드 상관 확인 (신뢰할 수 있는 CV 구축).
- 과적합 관리: early stopping, `num_leaves`/`min_child_samples` 등.
- 1일 5회 → **로컬 CV로 후보 선별 후 제출**.

### [7] 제출 파이프라인
- 학습 피처 == 추론 피처 (`build_features()` 공유).
- 10분/245K, 10GB 점검.
- → `06_제출체크리스트.md`

### [8] 반복 개선
- 실험 로그 관리 (무엇을 바꿔 몇 점 → `05_실험로그.md`)
- 제출은 하루 5회 → 가장 유망한 것만.

---

## Part B. 고려해볼 인프라 세팅 ⚙️

### B-1. CUDA / GPU를 써야 하는가? 🤔

**결론: 학습엔 선택적, 추론엔 "안 써도 되게" 설계 권장.**

| 상황 | GPU(CUDA) 필요성 | 판단 |
| --- | --- | --- |
| GBDT 학습 (LightGBM/XGB) | 선택 | 147만 행 → **CPU로도 충분히 학습 가능**. GPU 쓰면 빠르지만 필수 아님 |
| GBDT 추론 (24.5만 행) | 거의 불필요 | CPU로 10분 내 여유. **GPU 의존 제거가 안전** |
| 딥러닝 모델 | 사실상 필요 | 하지만 이 대회 우선순위 낮음 |

**핵심 판단:**
- 제출환경에 L4 GPU가 있지만, **추론에서 GPU 의존성을 만들면 리스크↑**
  (드라이버/CUDA 버전 mismatch → 실행 실패 가능).
- ✅ **추론(script.py)은 CPU만으로 동작하도록 작성** 권장.
  - GBDT는 CPU 예측이 빠르고 안정적.
- 학습 단계에서만 GPU 가속을 쓰고 싶다면 → 학습은 로컬/노트북에서,
  **저장된 모델은 CPU 추론 호환**으로.

> ⚠️ 주의: LightGBM을 `device=gpu`로 **학습**해도, 저장 모델은
> CPU 추론 가능. 단, 재현성을 위해 **학습·추론 환경 버전을 맞추는 것**이 우선.

---

### B-2. 환경 재현성 (오프라인 대비) 🔌

제출환경은 **오프라인 + pip install 제약**. 따라서:

1. **requirements.txt 최소화 + 버전 고정**
   ```
   numpy==x.x.x
   pandas==x.x.x
   scikit-learn==x.x.x
   lightgbm==x.x.x
   ```
   - 실제 제출환경에서 설치 가능한 표준 버전만 사용.
   - 버전 불명확 시 **널리 쓰이는 안정 버전** 선택.

2. **의존성 최소 원칙**
   - 학습에만 쓰는 무거운 라이브러리(optuna 등)는 **추론 코드에 넣지 않음**.
   - script.py는 `pandas`, `numpy`, `lightgbm`(또는 xgboost) 정도로.

3. **모델·사전집계 파일은 model/ 에 동봉**
   - `model.txt`(LightGBM native) 또는 `model.pkl`
   - 사전집계: `pitcher_agg.parquet`, `mapping.pkl` 등
   - 로드 경로는 **상대경로** (`./model/...`).

---

### B-3. 메모리 / 시간 예산 (28GB / 10분) 💾⏱️

| 자원 | 예산 | 대응 |
| --- | --- | --- |
| RAM 28GB | train 147만×49 로드 OK | dtype 최적화(float32, category)로 여유 확보 |
| 추론 10분/245K | 넉넉함 | 단, 무거운 실시간 집계 금지 → 사전계산 dict lookup |
| zip 10GB | 넉넉함 | 대형 앙상블·중복 파일만 주의 |

**메모리 절약 팁:**
- CSV 읽을 때 `dtype` 지정 (int8/int16/float32/category).
- 사전집계는 **dict / 작은 parquet**로 저장해 추론 시 빠른 lookup.

---

### B-4. 권장 로컬 개발 세팅

```
프로젝트/
├─ data/
│   ├─ train.csv
│   ├─ test.csv          # 5건 샘플로 파이프라인 테스트
│   ├─ trackman_history.csv
│   └─ sample_submission.csv
├─ notebooks/            # EDA, 실험
├─ src/
│   ├─ features.py       # build_features() ← 학습/추론 공유 ★
│   ├─ train.py          # 학습 + model/ 저장
│   └─ validate.py       # 시즌 기반 CV
├─ model/                # 산출물 (제출에 포함)
├─ output/
│   └─ submission.csv
├─ script.py             # 추론 진입점 (features.py 재사용)
└─ requirements.txt
```

> ★ 핵심: `features.py`를 학습과 추론이 **공유**해야 일관성 사고 방지.

---

### B-5. 세팅 관련 사전 확인 리스트

```
□ 제출환경 파이썬/패키지 버전 확인 (공지·docs)
□ 추론 CPU-only로 동작하는지 로컬에서 검증
□ requirements.txt 최소화 & 버전 고정
□ 모든 경로 상대경로 (./data, ./model, ./output)
□ 5건 샘플 test.csv로 script.py end-to-end 성공
□ dtype 최적화로 메모리 여유 확보
□ 사전집계 파일 크기 합산 < 10GB
```

---

## 한 줄 요약

> **"학습은 유연하게(GPU 선택), 추론은 단단하게(CPU-only·상대경로·의존성 최소)."**
> 성능보다 **재현·안정 파이프라인을 먼저** 세운다.
```

---

## 작성 완료 ✅

**설계 포인트:**
- 🗺️ Part A: 8단계 로드맵 — 각 단계에 **연결 문서 번호** 표기해 문서 간 네비게이션 확보
- ⚙️ Part B: 요청하신 **CUDA/GPU 판단**을 표로 명확히 → **"추론은 CPU-only 권장"** 이 핵심 결론
- 🎯 검증 설계([2])와 학습-추론 일관성(B-4 ★)을 강조 — 대회에서 점수 갈리는 지점

> 💡 **가장 강조하고 싶은 것 2가지:**
> 1. **추론에서 GPU 의존성 만들지 말 것** — 버전 mismatch로 실행 자체가 실패하면 0점 사고
> 2. **`features.py` 학습/추론 공유** — 피처 불일치는 조용히 점수를 깎는 최악의 버그

---

⚠️ 확인 필요: 제출환경의 정확한 파이썬/패키지 버전과 "규칙상 허용 외부 데이터"의 범위는 대회 공지에서 반드시 확인하세요. 이 두 가지는 제가 추측할 수 없는 부분이고, 잘못 가정하면 오프라인 설치 실패(0점) 로 이어질 수 있습니다.