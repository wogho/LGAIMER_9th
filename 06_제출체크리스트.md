
```markdown
# 06. 제출 체크리스트 📝

> ⚠️ **주의**: 하루 제출 횟수는 **5회**로 제한되어 있습니다.  
> 사소한 경로 오류나 규칙 위반으로 기회를 날리지 않도록 제출 전 반드시 확인하세요.

---

## 1. 🚨 치명적 규칙 위반 점검 (가장 중요)

이 항목 중 하나라도 어기면 **실격**되거나 **0점** 처리될 수 있습니다.

- [ ] **테스트 데이터 독립성 보장**: `test.csv`의 행(row) 간 참조를 절대 하지 않았는가?
  - ❌ 금지: `test.groupby()`, `test.shift()`, `test.rolling()`, `test.mean()` 등
  - ✅ 허용: `train`이나 `trackman`에서 미리 계산하여 저장해둔 통계값(Dictionary, DataFrame)을 `map`이나 `merge`로 붙이는 것
- [ ] **오프라인 환경 준수**: 코드 내에 인터넷 연결이 필요한 부분이 없는가?
  - ❌ 금지: `requests`, `urllib`, `wget`, HuggingFace 모델 실시간 다운로드 등
- [ ] **외부 데이터 금지**: 대회에서 제공한 데이터 외의 다른 데이터를 사용하지 않았는가?
- [ ] **예측값 범위**: 최종 예측 확률값(`control_success`)이 **0.0 ~ 1.0** 사이인가?
- [ ] **결측치 처리**: 최종 `submission.csv`에 `NaN`이나 `Inf` 값이 없는가? (결측 시 기본 확률값으로 채우는 로직 필수)

---

## 2. 📁 파일 및 폴더 구조 점검

제출할 `.zip` 파일의 내부 구조가 정확히 아래와 같은지 확인합니다.  
*(주의: 폴더 자체를 압축하지 말고, 폴더 내부의 파일들을 선택해서 압축해야 합니다.)*

```text
submission.zip
 ├── script.py            # 실행 스크립트 (필수)
 ├── requirements.txt     # 패키지 목록 (필수)
 └── model/               # 모델 가중치 및 사전 집계 데이터 폴더 (필수)
      ├── lgbm_model.pkl
      ├── pitcher_stats.pkl
      └── ...
```

- [ ] `script.py` 파일이 최상단에 존재하는가?
- [ ] `requirements.txt` 파일이 최상단에 존재하는가?
- [ ] `model/` 폴더 안에 학습된 모델 파일과 필요한 사전 집계 파일이 모두 들어있는가?
- [ ] 압축 파일의 총 용량이 **10GB** 이하인가?

---

## 3. 💻 `script.py` 입출력 경로 점검

플랫폼 환경은 고정되어 있으므로, 경로가 정확해야 합니다.

- [ ] **입력 데이터 경로**: `./data/test.csv`를 읽어오도록 작성되었는가?
  ```python
  # 올바른 예시
  test_df = pd.read_csv('./data/test.csv')
  ```
- [ ] **출력 데이터 경로**: `./output/submission.csv`로 저장하도록 작성되었는가?
  ```python
  # 올바른 예시
  submission.to_csv('./output/submission.csv', index=False)
  ```
- [ ] **모델 로드 경로**: `./model/` 폴더에서 파일을 읽어오도록 작성되었는가?
  ```python
  # 올바른 예시
  model = joblib.load('./model/lgbm_model.pkl')
  ```

---

## 4. ⏱️ 성능 및 환경 점검

- [ ] **추론 시간**: 24.5만 행(test.csv)을 처리하는 데 **10분** 이내로 완료되는가? (로컬에서 반드시 시간 측정)
- [ ] **메모리**: 28GB RAM 환경에서 Out of Memory(OOM)가 발생하지 않는 구조인가? (불필요한 데이터프레임은 `del` 및 `gc.collect()` 처리)
- [ ] **requirements.txt**: `script.py` 실행에 필요한 패키지와 버전이 정확히 명시되어 있는가?
  ```text
  pandas==2.1.4
  numpy==1.26.4
  scikit-learn==1.2.2
  lightgbm==4.1.0
  joblib==1.2.0
  ```

---

## 5. 🧪 로컬 모의 실행 (Dry Run)

제출 전 로컬에서 플랫폼과 동일한 폴더 구조를 만들고 실행해 봅니다.

1. 로컬에 빈 폴더 생성
2. `data/` 폴더 생성 후 `test.csv` (또는 train의 일부를 잘라 만든 가짜 test) 넣기
3. `output/` 빈 폴더 생성
4. 제출할 `script.py`, `requirements.txt`, `model/` 배치
5. 터미널에서 실행: `python script.py`

- [ ] 에러 없이 끝까지 실행되는가?
- [ ] `./output/submission.csv` 파일이 정상적으로 생성되었는가?
- [ ] `submission.csv`의 컬럼이 `row_id`, `control_success` 딱 2개인가?
- [ ] `submission.csv`의 행 수가 `test.csv`의 행 수와 동일한가?

---

## 6. 📊 제출 후 기록 (05_실험로그.md 연동)

제출을 완료하고 점수가 나오면 다음 사항을 기록합니다.

- [ ] `05_실험로그.md`의 **9. 제출 실험 기록** 테이블에 제출 번호(SUB-XXX)와 점수(LB Score) 기록
- [ ] 로컬 Valid BSS 점수와 리더보드(LB) BSS 점수의 차이(Gap) 확인
  - Gap이 너무 크거나 방향이 반대라면(로컬은 올랐는데 LB는 떨어짐) **과적합** 또는 **Data Leakage(누수)** 의심!

---

### 💡 튜터의 팁
> "제출 파일 압축할 때 제일 많이 하는 실수가 `submission`이라는 폴더 자체를 우클릭해서 압축하는 거예요. 그러면 압축 풀었을 때 `submission/script.py`가 되어서 경로 에러가 납니다. 반드시 **파일 3개(`script.py`, `requirements.txt`, `model/`)를 드래그해서 선택한 뒤 압축**하세요!"
```

이 체크리스트를 활용하면 어이없는 실수로 귀중한 제출 기회를 날리는 일을 완벽하게 방지할 수 있습니다! 이제 문서화가 모두 끝났으니, 본격적으로 코드를 작성해 볼까요? 다음 단계로 넘어갈 준비가 되면 알려주세요! 😊