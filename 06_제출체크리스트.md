# 06. 제출 체크리스트

> 모든 제출물은 제출 시점부터 규칙을 준수해야 한다. 아래 필수 게이트 중 하나라도 실패하면 제출하지 않는다.
> 최종 재검증일: 2026-08-16 · 최신 제출 대상: `output/candidates/submit_regime_r_candidate.zip` · 공식 점수: `886.2488171351`

## 1. 평가 행 독립성

- [x] 특정 행의 예측은 해당 행의 입력과 공식 학습 데이터에서 고정한 정보만 사용한다.
- [x] test 기반 `groupby`, `rolling`, `expanding`, `lag`, `shift`가 없다.
- [x] test 평균, 빈도, 결측률, 분포, 순위 또는 분위수 기반 피처·후처리가 없다.
- [x] 동일 선수·팀·월·경기의 다른 test 행을 참조하지 않는다.
- [x] 추론 중 `fit`, `fit_transform`, `partial_fit` 또는 상태 갱신이 없다.
- [x] train과 test를 합쳐 통계·인코딩·전처리 파라미터를 계산하지 않는다.
- [x] test 예측값 전체의 분포를 이용한 재스케일링이나 확률 보정이 없다.

### 필수 자동검사

```bash
python scripts/verify_features.py
python scripts/verify_independence.py
```

- [x] 학습·추론 피처의 컬럼, 순서, dtype 및 값이 일치한다.
- [x] 전체 배치와 각 행 단독 예측이 허용 오차 내에서 같다.
- [x] 행 순서를 변경해도 원래 행의 예측이 같다.
- [x] 무관한 행을 추가해도 기존 행의 예측이 같다.
- [x] 제출 스크립트에 명백한 배치 집계·누적·재학습 호출이 없다.

## 2. 데이터 및 누수

- [x] 공식 학습 데이터에서만 두 모델과 시간 OOF 보정기를 적합했다.
- [x] 추론에서는 고정된 두 native 모델, 피처·앙상블 계약, 고정 global Platt 계수만 로드한다.
- [x] 현재 투구 이후의 결과, 실제 구종, 위치, 판정 또는 측정값을 사용하지 않는다.
- [x] 제공되지 않은 2025년 TrackMan 데이터를 사용하지 않는다.
- [x] 최종 구성은 TrackMan 이력을 사용하지 않아 미래 이력 혼입 경로가 없다.

## 3. 추론 코드 Fail-Fast

- [x] `test.csv`와 sample submission의 `row_id`에 결측·중복이 있으면 실패한다.
- [x] 입력과 제출 양식의 행 수 또는 ID 집합이 다르면 실패한다.
- [x] 예측 수가 입력 행 수와 다르면 실패한다.
- [x] 누락 예측을 placeholder 또는 기본 제출값으로 숨기지 않는다.
- [x] 예측에 NaN/Inf가 있거나 [0, 1] 범위를 벗어나면 실패한다.
- [x] 출력 컬럼은 `row_id`, `control_success` 두 개이며 입력 순서를 유지한다.

## 4. 실행 환경 및 제출 구조

```text
submission.zip
├── script.py
├── requirements.txt
└── model/
```

- [x] 입력 경로는 `./data/test.csv`이다.
- [x] 출력 경로는 `./output/submission.csv`이다.
- [x] 모델과 계약 파일은 `./model/`에서 로드한다.
- [x] 인터넷 또는 외부 API 호출이 없다.
- [x] ZIP은 `534,864` bytes로 10GB 이하이다.
- [x] 245,789행 추론은 재검증 `1.65`초·`1,013.43MB`로 한도 이내이다.
- [x] Python `3.11.15`, LightGBM `4.7.0`, CatBoost `1.2.10`에서 실행 가능하다.

## 5. 최종 실행 순서

```bash
.venv/bin/python scripts/verify_regime_final_candidate.py
.venv/bin/python scripts/build_regime_r_submit_zip.py
unzip -t output/candidates/submit_regime_r_candidate.zip
sha256sum output/candidates/submit_regime_r_candidate.zip
.venv/bin/python scripts/audit_checklists.py
```

후속 후보는 기존 스크립트·ZIP을 덮어쓰지 않고 새 실험 ID의 verifier·builder·ZIP을 만든다.

- [x] 모든 명령이 종료 코드 0으로 완료됐다.
- [x] 검증한 ZIP과 실제 업로드 대상 ZIP이 SHA-256 `62de6d96...71993aa8`로 동일하다.
- [x] 최종 모델·피처·앙상블 활성화 후 모든 검사를 다시 실행했다.
- [x] 제출 후 실험 번호, 모델 버전, 검증 결과 및 리더보드 점수(`SUB-002`: `886.2488171351`)를 `05_실험로그.md`에 기록했다.

> 생성 AI가 작성한 코드도 참가자가 전체 추론 경로를 직접 검토해야 하며, 제출 후 삭제 요청은 규칙 검토 면책 사유가 아니다.

## 6. 1,000점 목표 후보 추가 게이트

> 다음 제출부터는 `07_1000점_달성계획.md`의 Gate A/B와 아래 항목을 모두 확인한다.

- [x] 이전 공식 기준 제출 `SUB-001`·`815.20127`점과 rollback ZIP을 보존했다.
- [x] 현재 공식 최고 제출을 `SUB-002`·`886.2488171351`점으로 동결했다.
- [x] 최소 목표 `1,000`점과 현재 필요 개선폭 `113.7511828649`점을 기록했다.
- [x] 후보의 단일 핵심 가설과 실험 ID(`REGIME-R-001`)가 정의돼 있다.
- [x] 2022·2023·2024 expanding-window 결과가 현재 선택형 기준과 paired 비교됐다.
- [x] 후보가 성능 Gate A를 통과했다.
- [x] 공식 train 이외 데이터·외부 API·test 분포를 사용하지 않았다.
- [x] 행 단독·순열·무관행 추가 독립성 검사가 모두 PASS다.
- [x] 245,789행 benchmark와 제출 ZIP 격리 E2E가 PASS다.
- [x] 후보 모델·피처·코드·ZIP SHA-256과 rollback 대상을 기록했다.
- [x] SUB-002 제출 전 플랫폼에서 제출 가능 상태를 확인했다.
- [x] 제출 직후 점수와 가설 판정을 `05_실험로그.md`에 기록했다.

## 7. SUB-003 이후 개정 게이트

- [ ] SUB-002와 동일한 행·OOF에서 paired 비교한다.
- [ ] 2024 Brier 개선을 원칙적으로 `0.00010` 이상 확보한다.
- [ ] 2023 단독 대폭 개선이 평균을 지배하지 않는지 확인한다.
- [ ] 2022 악화는 `0.00005` 이하로 제한한다.
- [ ] 후보의 최악 시즌 손실과 calibration gap을 함께 기록한다.
- [ ] LB 점수만 이용한 연속 가중치·보정계수 탐색을 하지 않는다.
- [ ] 단일 구조 가설, 중단 기준, rollback ZIP을 제출 전에 선언한다.


---

## 8. 최종 공식 제출물 완결 검증 기록 (`submit_ref4_super113A.zip`)

> **최종 제출일**: 2026-08-31 / 2026-09-01  
> **공식 최종 점수**: **`1121.9039933605`** 점 (Public 180위)  
> **최종 제출 패키지**: `output/submit_ref4_super_ensemble_113A.zip` (`submit_ref4_super113A.zip`)  
> **상태**: **`AUDIT_VERIFIED (ALL PASS)`**

- [x] **행 독립성 (Row-Independence)**: 단일 행 격리 오차 `0.000e+00` (PASS)
- [x] **순열 불변성 (Shuffled test)**: max |diff| = `0.000e+00` (PASS)
- [x] **Fail-Fast 무결성 계약**: NaN/Inf/범위 이탈 즉시 감지 (PASS)
- [x] **실행 환경 호환성**: Ubuntu 22.04 / Python 3.11 / CatBoost 1.2.10 / requirements.txt 완비 (PASS)
- [x] **추론 시간**: 245K행 기준 ~1분 완료 (10분 한도 여유) (PASS)
- [x] **솔루션 발표자료 동봉 및 문서화**: `LG_Aimers_솔루션_PPT_Phase2.pptx`, `10_솔루션_발표자료_요약.md` 완비 (PASS)
