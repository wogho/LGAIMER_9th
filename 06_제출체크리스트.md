# 06. 제출 체크리스트

> 모든 제출물은 제출 시점부터 규칙을 준수해야 한다. 아래 필수 게이트 중 하나라도 실패하면 제출하지 않는다.

## 1. 평가 행 독립성

- [ ] 특정 행의 예측은 해당 행의 입력과 공식 학습 데이터에서 고정한 정보만 사용한다.
- [ ] test 기반 `groupby`, `rolling`, `expanding`, `lag`, `shift`가 없다.
- [ ] test 평균, 빈도, 결측률, 분포, 순위 또는 분위수 기반 피처·후처리가 없다.
- [ ] 동일 선수·팀·월·경기의 다른 test 행을 참조하지 않는다.
- [ ] 추론 중 `fit`, `fit_transform`, `partial_fit` 또는 상태 갱신이 없다.
- [ ] train과 test를 합쳐 통계·인코딩·전처리 파라미터를 계산하지 않는다.
- [ ] test 예측값 전체의 분포를 이용한 재스케일링이나 확률 보정이 없다.

### 필수 자동검사

```bash
python scripts/verify_features.py
python scripts/verify_independence.py
```

- [ ] 학습·추론 피처의 컬럼, 순서, dtype 및 값이 일치한다.
- [ ] 전체 배치와 각 행 단독 예측이 허용 오차 내에서 같다.
- [ ] 행 순서를 변경해도 원래 행의 예측이 같다.
- [ ] 무관한 행을 추가해도 기존 행의 예측이 같다.
- [ ] 제출 스크립트에 명백한 배치 집계·누적·재학습 호출이 없다.

## 2. 데이터 및 누수

- [ ] 공식 학습 데이터에서만 통계, 집계표, 스케일러, 인코더 및 보정기를 학습했다.
- [ ] 추론에서는 저장된 학습 결과를 조회·변환에만 사용한다.
- [ ] 현재 투구 이후의 결과, 실제 구종, 위치, 판정 또는 측정값을 사용하지 않는다.
- [ ] 제공되지 않은 2025년 TrackMan 데이터를 사용하지 않는다.
- [ ] 공식 제공 과거 이력도 현재 예측 시점 이후 정보가 섞이지 않도록 검증했다.

## 3. 추론 코드 Fail-Fast

- [ ] `test.csv`와 sample submission의 `row_id`에 결측·중복이 있으면 실패한다.
- [ ] 입력과 제출 양식의 행 수 또는 ID 집합이 다르면 실패한다.
- [ ] 예측 수가 입력 행 수와 다르면 실패한다.
- [ ] 누락 예측을 placeholder 또는 기본 제출값으로 숨기지 않는다.
- [ ] 예측에 NaN/Inf가 있거나 [0, 1] 범위를 벗어나면 실패한다.
- [ ] 출력 컬럼은 `row_id`, `control_success` 두 개이며 입력 순서를 유지한다.

## 4. 실행 환경 및 제출 구조

```text
submission.zip
├── script.py
├── requirements.txt
└── model/
```

- [ ] 입력 경로는 `./data/test.csv`이다.
- [ ] 출력 경로는 `./output/submission.csv`이다.
- [ ] 모델과 사전 계산 파일은 `./model/`에서 로드한다.
- [ ] 인터넷 또는 외부 API 호출이 없다.
- [ ] ZIP은 10GB 이하이다.
- [ ] 245,789행 추론이 10분 이내이고 메모리가 28GB 이하이다.
- [ ] 제출 환경 Python 3.11 및 필요한 패키지 버전에서 실행 가능하다.

## 5. 최종 실행 순서

```bash
python scripts/validate_env.py
python scripts/verify_features.py
python scripts/verify_independence.py
python scripts/dry_run.py
python scripts/dry_run.py --benchmark
python scripts/build_submission.py --name <experiment_name>
```

`build_submission.py`는 ZIP 구조, 격리 E2E 출력 및 행 독립성을 다시 검사한다. 이 단계가 실패하면 생성 ZIP을 삭제하고 비정상 종료한다.

- [ ] 모든 명령이 종료 코드 0으로 완료됐다.
- [ ] 검증한 ZIP과 실제 업로드할 ZIP이 동일하다.
- [ ] 모델·피처·보정 로직 변경 후 모든 검사를 다시 실행했다.
- [ ] 제출 후 실험 번호, 모델 버전, 검증 결과 및 리더보드 점수를 `05_실험로그.md`에 기록한다.

> 생성 AI가 작성한 코드도 참가자가 전체 추론 경로를 직접 검토해야 하며, 제출 후 삭제 요청은 규칙 검토 면책 사유가 아니다.
