# 01. 제약 및 금지사항 — 위반 시 실격 주의

> LG Aimers 9기 온라인 해커톤(Phase 2)의 제출 코드 모니터링 공지를 반영한 필수 준수 기준이다.
> 생성 AI가 작성한 코드를 포함해 최종 제출 코드의 규칙 준수 책임은 참가자 본인에게 있다.

## 1. 최우선 원칙: 평가 데이터의 각 행은 독립적으로 예측

평가 데이터의 특정 행 A에 대한 예측값은 다음 정보만으로 생성해야 한다.

- 행 A에 포함된 입력 변수
- 행 A의 입력 변수만으로 만든 파생변수
- 주최 측이 제공한 공식 학습 데이터
- 공식 학습 데이터만으로 미리 생성한 통계, 모델, 인코더, 보정기 및 파생변수

같은 `test.csv`에 있는 다른 행은 시점, 선수, 팀, 월 또는 경기 관계와 무관하게 행 A의 추론에 사용할 수 없다. 평가 데이터상 더 과거로 보이는 행도 사용할 수 없다.

### 반드시 성립해야 하는 불변성

동일한 행 A는 아래 경우 모두 같은 예측값을 가져야 한다.

1. `test.csv`에 행 A만 있을 때
2. 행 A가 전체 평가 데이터와 함께 있을 때
3. 평가 행의 순서가 바뀌었을 때
4. 다른 평가 행이 추가, 제거 또는 복제되었을 때

현재 행의 예측은 평가 배치의 크기, 구성 및 순서에 의존하면 안 된다.

## 2. 평가 데이터에서 금지되는 처리

다음 처리는 피처 생성뿐 아니라 예측 후처리와 확률 보정에서도 금지한다.

- `test.csv`의 다른 행을 이용한 누적 통계
- 평가 행 기반 `groupby`, `rolling`, `expanding`, `lag`, `shift`
- 평가 데이터 전체의 평균, 중앙값, 표준편차, 분위수 또는 결측률 사용
- 평가 데이터 내부의 빈도 인코딩, 순위, 백분위 또는 분포 기반 정규화
- 같은 선수, 팀, 월, 경기 또는 임의 그룹의 평가 행 집계
- 평가 데이터 내 앞선 시점의 행을 현재 행의 이력처럼 사용
- 평가 데이터로 다시 학습하거나 갱신한 target encoding, 군집, 임베딩 또는 보정기
- 평가 예측값 전체의 평균·순위·분포를 이용한 재스케일링 또는 보정
- train과 test를 합친 뒤 계산한 통계, 인코딩 또는 전처리 파라미터

### 금지 코드 패턴 예시

```python
# 모두 금지: df가 평가 데이터인 경우
df.groupby("pitcher_id")["some_col"].transform("mean")
df.sort_values("date").groupby("pitcher_id")["some_col"].shift(1)
df["some_col"].rolling(10).mean()
df["pitcher_freq"] = df["pitcher_id"].map(df["pitcher_id"].value_counts())
pred = pred / pred.mean()
pred = pd.Series(pred).rank(pct=True)
```

`asof_*`처럼 주최 측이 현재 행의 공식 입력 변수로 제공한 값은 사용할 수 있지만, 평가 데이터의 다른 행을 이용해 이를 재계산하거나 보완하면 안 된다.

## 3. 허용되는 처리

- 현재 행 내부 값만 사용하는 산술 연산, 문자열 처리, 범주 조합 및 결측 플래그
- 공식 학습 데이터만으로 학습한 모델의 추론
- 공식 학습 데이터만으로 미리 계산해 저장한 집계표, 사전, 스케일러, 인코더 및 확률 보정기 적용
- 학습 데이터에서 정한 고정 fallback 값 적용
- 공식 제공 과거 이력 데이터가 대회 규칙상 허용되는 경우, 해당 데이터만으로 사전 계산한 통계를 현재 행의 키로 조회
- 행별 모델 예측의 고정 가중 평균 또는 학습 데이터에서 확정한 고정 수식 적용

사전 집계값을 `map` 또는 `merge`로 붙일 때 오른쪽 테이블은 공식 학습 데이터만으로 만들어 제출물의 `model/` 등에 저장해야 한다. 평가 데이터에서 오른쪽 집계표를 만들면 안 된다.

## 4. 미래 정보 및 정답 누수 금지

현재 투구 시점 이후에 확정되는 정보는 입력으로 사용할 수 없다.

- 현재 투구의 실제 위치 또는 코스
- 현재 투구의 실제 판정, 결과 또는 제구 성공 여부
- 현재 투구의 실제 구종
- 현재 투구 이후 생성되는 TrackMan 측정값
- 제공되지 않은 2025년 TrackMan 데이터
- 그 밖에 정답을 직접 또는 간접적으로 복원할 수 있는 정보

`trackman_history.csv` 등 공식 제공 과거 이력 데이터를 사용할 때도 현재 예측 시점 이후의 정보가 섞이지 않도록 학습 단계에서 시점 기준을 검증해야 한다.

## 5. 생성 AI 및 제출 책임

- 생성 AI를 이용한 코드 작성 자체는 금지되지 않는다.
- 생성 코드의 데이터 출처와 계산 범위를 참가자가 직접 확인한다.
- “과거 시점처럼 보인다”, “타깃을 쓰지 않는다”, “성능 향상용 후처리다”는 이유로 평가 행 간 참조가 허용되지 않는다.
- 규칙을 숙지하지 못했거나 생성 코드를 그대로 사용했다는 사유는 면책 사유가 아니다.
- 제출 후 제외 또는 삭제를 요청해도 이미 제출한 코드는 모니터링 및 규칙 검토 대상에 포함된다.
- 운영진은 제출 코드와 모델의 추론 과정을 검토할 수 있으며, 위반 시 규정에 따라 실격될 수 있다.

## 6. 실행 및 제출 형식 제약

| 항목 | 기준 |
|---|---|
| 실행 시간 | 245,789행 기준 10분 이내 |
| 평가 환경 | Ubuntu 22.04, Python 3.11.15, 6 vCPU, RAM 28GB, L4 GPU 1개 |
| 네트워크 | 오프라인 |
| 제출 횟수 | 하루 5회 |
| 제출물 크기 | ZIP 10GB 이내 |
| 입력 | `./data/test.csv` |
| 출력 | `./output/submission.csv` |

```text
submission.zip
├── script.py
├── requirements.txt
└── model/
```

출력은 `row_id`, `control_success` 두 컬럼으로 구성한다. `row_id`의 순서·개수·값은 평가 입력과 일치해야 하며, `control_success`는 NaN/Inf가 없는 0 이상 1 이하의 실수여야 한다.

## 7. 학습-추론 일관성

- 학습과 추론에서 동일한 피처 정의, 컬럼 순서 및 dtype을 사용한다.
- 학습 데이터로 계산한 통계, smoothing 값, 결측 fallback, 인코더 및 보정기는 학습 후 고정하여 저장한다.
- 추론 시 `fit`, `fit_transform`, 통계 재계산 또는 상태 갱신을 수행하지 않는다.
- 배치 단위로 상태가 달라지는 사용자 정의 transformer나 모델이 없는지 확인한다.
- `row_id`는 출력 정렬용 식별자로만 사용한다.

## 8. 제출 전 행 독립성 검증

```python
pred_full = predict(sample_df)
pred_single = np.array([
    predict(sample_df.iloc[[i]])[0]
    for i in range(len(sample_df))
])

permuted = sample_df.sample(frac=1, random_state=42)
pred_permuted = predict(permuted)
pred_restored = pd.Series(pred_permuted, index=permuted.index).sort_index().to_numpy()

np.testing.assert_allclose(pred_full, pred_single, rtol=0, atol=1e-12)
np.testing.assert_allclose(pred_full, pred_restored, rtol=0, atol=1e-12)
```

코드 검색 결과만으로 준수를 확정하지 말고 모델 및 사용자 정의 transformer 내부 동작도 확인한다. 현재 인프라에서는 다음 자동검사를 모두 통과해야 한다.

```bash
python scripts/verify_features.py
python scripts/verify_independence.py
python scripts/dry_run.py
python scripts/build_submission.py --name <experiment_name>
```

`verify_independence.py`는 실제 제출 코드로 단독 행, 전체 배치, 순서 변경, 무관한 행 추가를 비교하며 명백한 배치 통계·누적·재학습 호출도 정적으로 차단한다. 최종 ZIP 빌드 시 이 검사는 우회할 수 없는 필수 게이트로 다시 실행된다.

### 최종 체크리스트

- [x] 동일 행의 단독 예측과 전체 배치 예측이 같다.
- [x] 행 순서를 바꾸거나 다른 행을 추가·제거·복제해도 기존 행의 예측이 같다.
- [x] 평가 데이터 기반 집계, 빈도, 분포, 순위, 시계열 피처가 없다.
- [x] 평가 예측 전체를 이용한 보정이나 후처리가 없다.
- [x] 모든 전처리기와 보정기는 공식 학습 데이터에서만 학습되어 고정돼 있다.
- [x] 미래 정보 및 정답 누수 피처가 없다.
- [x] 학습과 추론의 피처 계약이 일치한다.
- [x] 출력 형식, 경로, 실행 시간, 메모리 및 오프라인 조건을 충족한다.

> 2026-08-16 최종 업로드 전 전수 감사에서 정적 코드 검사, 5행·50행 동적 독립성, 12개 Fail-Fast 변형, 245,789행 benchmark와 최종 ZIP sandbox를 재검증했다. 상세 증거는 `output/final_preupload_audit.json`, `output/final_preupload_audit.md`, `output/final_failfast_audit.json`에 보존한다.

> **사전 계산은 공식 학습 데이터에서만 하고, 추론 결과는 현재 평가 행 하나만으로 결정한다.**

## 9. 데이콘 운영진 추가 공식 안내 — 평가 정답 준거 정보 및 Phase 3 검증

다음 내용은 대회 운영진의 공식 답변을 반영한 추가 제약이다.

### 9.1 평가 정답 또는 정답 준거 정보 취득·추정·도출 금지

- 평가 데이터의 실제 결과, 정답 또는 정답에 준하는 정보를 외부에서 취득할 수 없다.
- 평가 데이터가 비식별화되어 있더라도, 특정 샘플의 정답을 비정상적인 방법으로 식별·추정·도출하여 사용하면 규칙 위반이다.
- 외부 정보뿐 아니라 평가 정답에 준하는 정보를 이용한 다음 행위도 금지한다.
  - 예측값 임의 조정 및 후처리값 튜닝
  - 앙상블 비율·가중치 조정
  - Feature 설계·선택·변환
  - 모델 설정·하이퍼파라미터 조정
- 정상적인 공식 학습 데이터 기반 모델 학습·추론과 구분되는 모든 정답 준거 조작은 금지한다.

### 9.2 제출 코드뿐 아니라 전체 과정 검증

Phase 3 진출 검증은 추론 코드가 점수를 재현하는지만 확인하지 않는다. 운영진은 다음 전체 과정을 종합적으로 확인할 수 있다.

- 학습 코드와 추론 코드
- 활용 데이터와 데이터 출처
- 주요 Feature 생성 과정
- 후처리값 및 보정값
- 앙상블 비율·가중치
- 모델 설정 및 하이퍼파라미터
- 학습·추론 과정의 재현성 및 근거

### 9.3 순위와 무관한 선발 제외

- 높은 리더보드 점수라도 규칙 위반 방식으로 얻은 결과는 최종 선발에 인정되지 않는다.
- 규칙 위반이 확인되거나 전체 학습·추론 과정을 정상적으로 재현하지 못하면, 리더보드 순위와 관계없이 Phase 3 대상자에서 제외될 수 있다.
- 운영진은 검증 우회에 악용될 수 있으므로 개별 위반 방식과 세부 검증 내용을 일률적으로 공개하지 않을 수 있다.

### 9.4 내부 작업 적용 규칙

- 리더보드 점수에서 평가셋 정답·성공률·예측 평균·분포·보정 파라미터를 역산하지 않는다.
- 공식 학습 데이터에서 사전에 고정하지 않은 후처리·앙상블·Feature를 평가 점수만 보고 선택하지 않는다.
- 모든 후보는 학습 코드, 추론 코드, 데이터 출처, 파라미터 산출 근거를 함께 보존한다.
- 증거가 없는 `PASS`·`대폭 개선`·`재현 가능` 판정을 금지하고, 검증 미완료 후보는 `HOLD`로 보고한다.

> 운영진 공식 답변 요지: 정상적인 학습·추론 절차를 벗어나 평가 데이터의 실제 결과 또는 정답에 준하는 정보를 취득·추정·도출하여 예측에 활용하는 모든 행위는 규칙 위반이다.
