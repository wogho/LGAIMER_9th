```markdown
# 01. 제약 및 금지사항 — 위반 시 실격 주의 ⚠️

> 이 문서는 "하면 안 되는 것"과 "지켜야 하는 것"만 모았다.
> 제출 직전, 모델 설계 전에 반드시 다시 읽는다.

---

## 1. 실행 환경 제약 (기술적 한계)

| 항목 | 제약 |
| --- | --- |
| 실행 시간 | **10분 / 245K 샘플** |
| CPU | 6 vCPU |
| RAM | 28 GB |
| GPU | L4 GPU 1개 |
| 네트워크 | **오프라인** (외부 접속 불가) |
| 제출 횟수 | **1일 5회** |
| 제출물 크기 | **10GB zip 이내** |

### 환경 제약이 주는 실전 함의
- ⏱️ **10분 안에 24.5만 행 추론 완료** → 무거운 전처리·대형 앙상블 주의.
- 💾 모델 + 사전집계 파일 합쳐 **10GB 이내** → 대용량 pickle/parquet 관리.
- 🔌 오프라인 → pip install 불가. **모든 의존성은 제출 환경에 이미 있어야 함**
  (`requirements.txt`로 명시하되, 설치 가능한 표준 패키지만 가정).
- 🔁 **1일 5회 제한** → 무작정 제출 금지. 로컬 검증으로 후보를 걸러낸 뒤 제출.

---

## 2. 제출물 구조 규칙

### 필수 zip 구조
```
제출.zip
  ├─ model/              # 학습된 모델 + 사전계산 파일
  ├─ script.py           # 추론 스크립트
  └─ requirements.txt    # 의존성 명시
```

### 입출력 경로 (고정)
| 항목 | 경로 |
| --- | --- |
| 입력 | `./data/test.csv` |
| 출력 | `./output/submission.csv` |

### 지켜야 할 것
- `script.py`는 `./data/test.csv`를 읽어 `./output/submission.csv`를 생성해야 한다.
- 출력 컬럼은 `row_id`, `control_success` **2개**.
- `row_id`는 평가 서버 `test.csv`와 **정확히 일치** (순서·개수·값).
- 예측값 `control_success`는 **0 이상 1 이하 실수**.

---

## 3. 데이터 사용 규칙 — 절대 위반 금지 🚨

### 3-1. 평가 데이터(test) 행 간 참조 금지
> 평가 데이터의 각 행은 **독립적으로** 예측해야 한다.
> test.csv 전체가 주어지더라도, **다른 행을 이용해 현재 행의 피처를 만들 수 없다.**

**금지되는 예시 (전부 실격 사유):**
- ❌ test 내부의 **선수별·팀별·월별 누적 통계**
- ❌ test 내부 **빈도값 / 분포 통계**
- ❌ test 내부 **target encoding**
- ❌ test 내부 **행 순서 기반 rolling / expanding feature**
- ❌ 평가 데이터 전체를 보고 만든 **사후 보정값**

**허용:**
- ✅ `train.csv` / `trackman_history.csv`(2019~2024)로 **사전 집계**한 값을
  test 각 행에 **단순히 붙이는(map/join)** 방식
- ✅ 운영 측 제공 `asof_*` 컬럼 (공식 입력 피처)

### 3-2. 사용 금지 정보 (미래·정답 누수)
현재 투구 시점 이후에 확정되는 정보는 **입력 사용 불가**:

- ❌ 현재 투구 **이후**에 확정되는 모든 정보
- ❌ 현재 투구의 **실제 위치/코스**
- ❌ 현재 투구의 **실제 판정/결과/제구 성공 여부**
- ❌ 현재 투구의 **실제 구종**
- ❌ 현재 투구의 **Trackman 측정값**
- ❌ **2025년 Trackman 데이터** (애초에 제공 안 됨)

### 3-3. 외부 데이터
- ❌ 외부 API / 인터넷 데이터 (오프라인 환경)
- ✅ 대회 규칙상 허용되는 외부 데이터만 사용 가능
  (※ 허용 범위 불명확 시 사용하지 않는 것이 안전)

---

## 4. 사용 가능한 데이터 (전부 명시)

| 데이터 | 사용 가능 | 비고 |
| --- | --- | --- |
| `train.csv` | ✅ | 학습 + 사전집계 소스 |
| 평가환경 `test.csv` | ✅ (단, **행 독립**) | 현재 행 피처만 |
| `trackman_history.csv` (2019~2024) | ✅ | 과거 이력 피처 소스, 미래 정보로 사용 불가 |
| 2025 Trackman | ❌ | 제공 안 됨 |
| 규칙상 허용 외부 데이터 | △ | 범위 확인 후 |

---

## 5. Trackman 사용 시 주의

- ⚠️ `trackman_history.csv`는 train/test와 **1:1 결합 정답 테이블이 아님**.
  → 과거 이력 요약 피처 생성용으로만 사용.
- ⚠️ Trackman ID(`pitcher_trackman_id` 등)는 메인 데이터 익명 ID와
  **다른 체계일 수 있음** → 조인 가능 여부는 EDA로 검증 필요.
- ⚠️ Trackman을 통해서도 **현재 투구 이후·측정값 누수**가 발생하지 않도록 주의.

---

## 6. 학습-추론 일관성 (실수 최다 발생 구간) 🔁

> 학습 시 만든 피처와 추론(script.py) 시 만드는 피처가 **완전히 동일**해야 한다.

- 학습 노트북의 `build_features()` == `script.py`의 `build_features()`
- 사전집계 테이블(smoothing 파라미터, 결측 fallback 값 등)은
  **train으로 계산해 model/에 저장** → 추론 시 그대로 로드.
- 추론 중 **test 데이터로 통계를 재계산하지 않는다** (3-1 위반).

---

## 7. 제출 전 최종 점검 (요약 체크)

```
□ 행 독립 원칙 준수 (test 행 간 참조 없음)
□ 미래/정답 누수 피처 없음
□ 2025 trackman 미사용
□ 사전집계는 train/trackman(19~24)에서만
□ script.py 피처 = 학습 피처 동일
□ 입력 ./data/test.csv, 출력 ./output/submission.csv
□ 출력 = row_id + control_success (0~1)
□ row_id 순서/개수 test와 일치
□ zip = model/ + script.py + requirements.txt
□ 10GB 이내 / 10분 내 추론
□ 오프라인 실행 가능 (설치 없이)
```
> 상세 체크리스트는 `06_제출체크리스트.md` 참고.

---

## 8. 한 줄 원칙

> **"사전 집계는 과거(train/trackman)에서만, 추론은 한 행씩 독립적으로."**
```

---

## 작성 완료 ✅

**핵심 설계:**
- 🔴 **3장(데이터 사용 규칙)** 을 가장 강조 — 실격과 직결되는 부분
- 🔁 **6장(학습-추론 일관성)** 추가 — 규칙 위반은 아니지만 **점수 폭락 사고**가 가장 자주 나는 지점이라 넣었습니다
- ⚠️ 원문에 없는 제 해석(Trackman ID 체계, 외부데이터 범위)은 "확인 필요/안전 선택" 으로 명시

> 💡 **참고**: "규칙상 허용되는 외부 데이터"의 정확한 허용 범위는 대회 공지에서 확인하시는 게 좋습니다. 애매하면 **안 쓰는 게 안전**합니다.

---