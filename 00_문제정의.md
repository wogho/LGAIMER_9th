# 00. 문제 정의 — LG Aimers 야구 제구 성공 확률 예측 AI (Phase 2)

> **대회 결과**: 최종 공식 점수 **`1121.9039933605`** 점 (Brier Skill Score) / **Public 180위** 완주  
> **최종 제출 패키지**: `submit_ref4_super113A.zip` (`REF4-DISJOINT-EB-113A`)  
> **상세 솔루션 문서**: [README.md](README.md), [10_솔루션_발표자료_요약.md](10_솔루션_발표자료_요약.md)

---

## 1. 대회 개요

- **주제**: 야구 제구(control) 성공 확률 예측 AI
- **문제 유형**: 이진 분류의 확률값 예측 (0~1 실수 출력)
- **목표**: 투구 직전까지 알 수 있는 정보(경기 상황, 선수/팀 정보, 과거 이력)를 바탕으로,
  해당 투구의 **제구 성공 확률**을 예측한다.
- **예측 대상**: `control_success` (1=제구 성공, 0=제구 실패)의 성공 확률

---

## 2. 평가

- **평가지표**: Brier Skill Score (BSS)
- **기준 점수**:
  - 0점 = 기준선(baseline)
  - 549.51점 = 수료 기준
  - 100000점 = 완벽한 예측
- **평가 원칙**: 평가 데이터의 각 행은 **독립적으로** 예측해야 한다.
  - 평가 서버에서 실제 `test.csv` 전체가 주어지더라도,
    `test.csv`의 다른 행을 이용해 현재 행의 피처를 만들 수 없다.

---

## 3. 실행 환경 및 제약

- **시간 제한**: 10분 / 245K 샘플
- **자원**: 6 vCPU / 28GB RAM / L4 GPU
- **네트워크**: 오프라인 (외부 API/데이터 접근 불가)
- **제출 횟수**: 1일 5회
- **제출물 크기**: 10GB zip 이내

### 제출 파일 구조
```
제출.zip
  ├─ model/              # 학습된 모델 및 사전계산 파일
  ├─ script.py           # 추론 스크립트
  └─ requirements.txt    # 의존성 명시
```

- **입력 경로**: `./data/test.csv`
- **출력 경로**: `./output/submission.csv`

---

## 4. 배포용 데이터 구조

```text
open.zip

baseline_submit.zip : 베이스라인 코드 기반 리더보드 제출 파일(zip) 예시 (참고용)
data/
  - train.csv              : 학습 입력 및 정답 데이터 (1,475,092행 x 49컬럼)
  - test.csv               : 평가 입력 데이터 (형식 확인용 5건 샘플, 48컬럼)
  - sample_submission.csv  : 제출 양식 파일 (형식 확인용 5행 x 2컬럼)
  - trackman_history.csv   : 2019~2024년 Trackman 과거 로그 (1,793,078행 x 30컬럼)

data_description.md : 데이터 설명서
```

### 중요 참고사항
- `test.csv`는 배포본에 **형식 확인용 5건**만 포함된다.
  - 실제 평가 데이터는 **비공개**이며, 제출용 zip을 리더보드에 제출하면
    평가 서버에서 **동일한 경로·동일한 컬럼 구조의 실제 평가 데이터로 교체**되어 처리된다.
- `sample_submission.csv`도 형식 확인용 5건 샘플이다.
  - 실제 평가 시에는 평가 서버의 `test.csv` 행 수와 동일한 제출 양식을 생성해 제출해야 한다.
- 모든 데이터 파일은 CSV 형식이다.
- `trackman_history.csv`는 메인 학습/평가 데이터와 **1:1로 결합하는 정답 테이블이 아니며**,
  참가자가 과거 이력 기반 피처를 만들 때 참고할 수 있는 로그 데이터이다.

---

## 5. 학습/추론 데이터: `train.csv` · `test.csv`

각 행은 **한 개의 투구 시점 상태**를 나타낸다.
참가자는 투구 직전까지 알 수 있는 경기 상황, 선수/팀 정보, 과거 이력 피처를 바탕으로
해당 투구의 제구 성공 확률을 예측한다.

- `train.csv`와 `test.csv`의 **입력 피처 구조는 동일**하다.
- `train.csv`에는 학습 정답 `control_success`가 포함된다.
- `test.csv`에는 정답 컬럼이 포함되지 않는다.

### 5-1. 기본 식별자 및 경기 정보

| 컬럼 | 설명 |
| --- | --- |
| `row_id` | 샘플 고유 식별자. 제출 파일과 매칭하는 데 사용. |
| `season` | 시즌 연도. |
| `game_month` | 경기 월. |
| `game_dayofweek` | 경기 요일. 월요일=0, 일요일=6. |
| `inning` | 투구 직전 이닝. |
| `top_bottom` | 초/말 구분. `T`=초, `B`=말. |
| `game_type` | 경기 유형 코드. |

### 5-2. 투구 직전 카운트 및 점수 상황

| 컬럼 | 설명 |
| --- | --- |
| `balls_before` | 투구 직전 볼 카운트. |
| `strikes_before` | 투구 직전 스트라이크 카운트. |
| `outs_before` | 투구 직전 아웃 카운트. |
| `run_top_before` | 투구 직전 초 공격 팀의 점수. |
| `run_bot_before` | 투구 직전 말 공격 팀의 점수. |
| `run_total_before` | 투구 직전 양 팀 합산 점수. |
| `score_diff_home` | 투구 직전 홈 팀 기준 점수 차. |
| `score_diff_pitcher_team` | 투구 직전 투수 소속 팀 기준 점수 차. |

### 5-3. 주자 및 상황 중요도

| 컬럼 | 설명 |
| --- | --- |
| `runner_on_1b` | 투구 직전 1루 주자 여부. 1=있음, 0=없음. |
| `runner_on_2b` | 투구 직전 2루 주자 여부. 1=있음, 0=없음. |
| `runner_on_3b` | 투구 직전 3루 주자 여부. 1=있음, 0=없음. |
| `num_runners_on` | 투구 직전 출루 주자 수. |
| `base_state` | 투구 직전 주자 상황. `___`=주자없음, `1__`=1루, `_2_`=2루, `__3`=3루, `12_`=1/2루, `1_3`=1/3루, `_23`=2/3루, `123`=만루. |
| `home_win_expectancy` | 투구 직전 홈 팀 기대 승률. 0~100 범위. |
| `away_win_expectancy` | 투구 직전 원정 팀 기대 승률. 0~100 범위. |
| `li` | 투구 직전 상황 중요도 지표. 값이 클수록 경기 흐름에 미치는 영향이 큰 상황. |

### 5-4. 선수 및 팀 정보

| 컬럼 | 설명 |
| --- | --- |
| `pitcher_id` | 투수 익명 ID. |
| `batter_id` | 타자 익명 ID. |
| `pitcher_hand` | 투수의 좌우 유형 코드. |
| `batter_hand` | 타자의 좌우 유형 코드. |
| `pitcher_team_id` | 투수 소속 팀 ID. |
| `batter_team_id` | 타자 소속 팀 ID. |

### 5-5. 투구 직전 기준 과거 이력 피처 (`asof_*`)

`asof_*` 컬럼은 해당 행의 투구 직전까지 확인 가능한 과거 기록으로 **사전 계산된 피처**이다.
현재 투구 이후에 확정되는 정보는 사용하지 않았다.

| 컬럼 | 설명 |
| --- | --- |
| `asof_pitcher_n` | 투구 직전까지 해당 투수의 누적 투구 수. |
| `asof_pitcher_success_rate` | 투구 직전까지 해당 투수의 제구 성공률. |
| `asof_pitcher_reverse_rate` | 투구 직전까지 해당 투수의 의도 반대성 투구 비율. |
| `asof_pitcher_middle_rate` | 투구 직전까지 해당 투수의 가운데 또는 위험 코스 비율. |
| `asof_pitcher_ball_rate` | 투구 직전까지 해당 투수의 볼성 결과 비율. |
| `asof_pitcher_strike_rate` | 투구 직전까지 해당 투수의 스트라이크성 결과 비율. |
| `asof_pitcher_prev1_game_success_rate` | 해당 투수의 직전 1경기 제구 성공률. |
| `asof_pitcher_prev3_game_success_rate` | 해당 투수의 직전 3경기 제구 성공률. |
| `asof_pitcher_prev5_game_success_rate` | 해당 투수의 직전 5경기 제구 성공률. |
| `asof_pitcher_prev1_game_middle_rate` | 해당 투수의 직전 1경기 가운데 또는 위험 코스 비율. |
| `asof_pitcher_prev3_game_middle_rate` | 해당 투수의 직전 3경기 가운데 또는 위험 코스 비율. |
| `asof_pitcher_prev5_game_middle_rate` | 해당 투수의 직전 5경기 가운데 또는 위험 코스 비율. |
| `asof_batter_n` | 투구 직전까지 해당 타자가 상대한 누적 투구 수. |
| `asof_batter_success_rate` | 투구 직전까지 해당 타자가 상대한 투구의 제구 성공률. |
| `asof_batter_middle_rate` | 투구 직전까지 해당 타자가 상대한 투구의 가운데 또는 위험 코스 비율. |
| `asof_pitcher_pitchmix_n` | 투구 직전까지 해당 투수의 구종 사용 이력 표본 수. |
| `asof_pitcher_fastball_rate` | 투구 직전까지 해당 투수의 fastball 계열 사용 비율. |
| `asof_pitcher_breaking_rate` | 투구 직전까지 해당 투수의 breaking 계열 사용 비율. |
| `asof_pitcher_offspeed_rate` | 투구 직전까지 해당 투수의 offspeed 계열 사용 비율. |

> ※ 표본 수가 0인 경우 일부 rate 컬럼은 **결측값**일 수 있다.
> 이런 cold-start 상황의 결측 처리, smoothing, fallback 전략은 참가자가 자유롭게 설계할 수 있다.

---

## 6. 학습 정답 데이터: `train.csv`의 `control_success`

| 컬럼 | 설명 |
| --- | --- |
| `control_success` | 예측 대상. 1=제구 성공, 0=제구 실패. |

- `control_success`는 운영 기준에 따라 산출된 제구 성공 여부이다.
- Target 산출에 사용되는 **현재 투구의 사후 정보는 입력 피처로 제공되지 않는다.**

---

## 7. 과거 Trackman 로그: `trackman_history.csv`

- `trackman_history.csv`는 **2019~2024년 Trackman 과거 로그**이다.
- **2025년 Trackman 데이터는 제공되지 않는다.**
- 참가자는 이 파일로 **과거 투구 특성, 구종 특성, 투수 단위 요약값** 등 추가 피처를 만들 수 있다.
- 단, 이 파일은 `train.csv` 또는 `test.csv`와 **1:1로 직접 결합되는 테이블이 아니며**,
  평가 시점 이후 정보를 포함하는 방식으로 사용할 수 없다.

| 컬럼 | 설명 |
| --- | --- |
| `trackman_id` | Trackman 과거 로그의 행 식별자. |
| `season` | Trackman 투구가 속한 시즌. 2019~2024만 포함. |
| `game_date` | Trackman 투구의 경기 날짜. |
| `game_month` | Trackman 투구의 경기 월. |
| `game_dayofweek` | Trackman 투구의 경기 요일. 월요일=0, 일요일=6. |
| `trackman_game_id` | Trackman 기준 경기 ID. 메인 데이터의 `row_id`와 직접 대응하지 않음. |
| `pitch_no` | Trackman 기준 경기 내 투구 번호. |
| `inning` | Trackman 로그의 이닝. |
| `top_bottom` | Trackman 로그의 초/말 표기. |
| `balls_before` | 투구 직전 볼 카운트. |
| `strikes_before` | 투구 직전 스트라이크 카운트. |
| `outs_before` | 투구 직전 아웃 카운트. |
| `pitch_of_pa` | 해당 타석에서의 투구 순번. |
| `pitcher_trackman_id` | Trackman 기준 투수 ID. |
| `batter_trackman_id` | Trackman 기준 타자 ID. |
| `pitcher_hand` | 투수의 좌우 유형 코드. |
| `batter_hand` | 타자의 좌우 유형 코드. |
| `pitcher_team` | 투수 소속 팀. |
| `batter_team` | 타자 소속 팀. |
| `tagged_pitch_type` | 수동 또는 태깅 기반 구종명. |
| `auto_pitch_type` | 자동 분류 기반 구종명. |
| `pitch_type_group` | 구종군: `fastball`, `breaking`, `offspeed`, `other`. |
| `rel_speed` | 릴리스 시점의 구속. |
| `spin_rate` | 투구 회전수. |
| `induced_vert_break` | 유도 수직 무브먼트. |
| `horz_break` | 수평 무브먼트. |
| `extension` | 릴리스 확장 거리. |
| `rel_height` | 릴리스 높이. |
| `rel_side` | 릴리스 좌우 위치. |
| `zone_speed` | 홈플레이트 근처 구속. |

> ⚠️ 주의: Trackman의 투수/타자 ID(`pitcher_trackman_id`, `batter_trackman_id`)는
> 메인 데이터의 익명 ID(`pitcher_id`, `batter_id`)와 **다른 ID 체계일 수 있다.**
> 직접 조인 가능 여부는 EDA에서 별도로 확인해야 한다. (→ `02_데이터탐색_EDA.md`)

---

## 8. 모델 추론 결과 양식: `sample_submission.csv`

| 컬럼 | 설명 |
| --- | --- |
| `row_id` | 평가 데이터 `test.csv`의 샘플 식별자. |
| `control_success` | 예측값. 제구 성공 확률을 0 이상 1 이하의 실수로 입력. |

- 제출 시 `row_id` 값은 평가 서버에서 제공되는 `test.csv`와 **정확히 일치**해야 한다.

---

## 9. 평가 데이터 예측 원칙

- 평가 데이터의 각 행은 **독립적으로** 예측해야 한다.
- 평가 서버에서 실제 `test.csv` 전체가 주어지더라도,
  `test.csv`의 다른 행을 이용해 현재 행의 피처를 만들 수 없다.

### 금지되는 예시
- `test.csv` 내부 행들을 이용한 **선수별·팀별·월별 누적 통계**
- `test.csv` 내부 **빈도값 또는 분포 통계**
- `test.csv` 내부 **target encoding**
- `test.csv` **행 순서 기반 rolling 또는 expanding feature**
- 평가 데이터 전체를 보고 만든 **사후 보정값**

### 허용
- 운영 측이 제공한 `asof_*` 컬럼은 각 행의 투구 직전 시점까지의 과거 기록만으로
  계산된 **공식 입력 피처**이므로 사용할 수 있다.

---

## 10. 사용 금지 정보

공정한 평가를 위해 다음 정보는 입력으로 사용할 수 없다.

- 현재 투구 **이후**에 확정되는 모든 정보
- 현재 투구의 **실제 위치 또는 코스 정보**
- 현재 투구의 **실제 판정, 결과, 제구 성공 여부**
- 현재 투구의 **실제 구종**
- 현재 투구의 **Trackman 측정값**
- **2025년 Trackman 데이터** (제공되지 않음)
- 평가 데이터 내부의 다른 행을 이용해 만든 누적·빈도·분포·rolling·target encoding 피처

### 사용 가능한 데이터 (전부)
- `train.csv`
- 평가 환경의 `test.csv`
- 2019~2024년 `trackman_history.csv`
- 대회 규칙상 허용되는 외부 데이터

---

## 11. 데이터 요약 (한눈에)

| 파일 | 행 수 | 컬럼 수 | 정답 포함 | 용도 |
| --- | --- | --- | --- | --- |
| `train.csv` | 1,475,092 | 49 | O (`control_success`) | 학습 |
| `test.csv` | (배포본 5건 / 실제 비공개) | 48 | X | 평가 |
| `trackman_history.csv` | 1,793,078 | 30 | - | 과거 이력 피처 생성 |
| `sample_submission.csv` | (5행 샘플) | 2 | - | 제출 양식 |

### train.csv 컬럼 구성 (49 = 1 + 47 + 1)
- `row_id` (1)
- 입력 피처 (47): 경기정보(6) + 카운트/점수(8) + 주자/중요도(9) + 선수/팀(6) + `asof_*`(19)
- `control_success` (1)

> ※ `test.csv`는 정답 컬럼이 없어 48컬럼 (= 49 − `control_success`).

---

## 12. 핵심 요약 (반드시 기억)

1. **미래 예측 구조**: 학습(2019~2024) → 평가(2025). 시즌 기반 검증 권장.
2. **행 독립 예측**: test는 한 행씩 독립. 사전 집계 테이블을 붙이는 방식만 안전.
3. **trackman은 2019~2024만**: 2025 없음. 직접 조인 여부는 확인 필요.
4. **asof_는 공식 허용 피처**: 마음껏 사용/파생 가능.
5. **평가지표는 Brier 계열**: 확률 캘리브레이션이 점수에 직접 영향.
```

---

## 작성 완료 ✅

보내주신 원문 내용을 **누락·왜곡 없이** 담았고, 다음만 명확히 구분해 표기했습니다:

- **원문 사실** → 그대로 기술
- **제 해석/주의** → `>` 인용 블록 + "확인 필요" 명시 (예: trackman ID 체계, 11번 컬럼 개수 계산)

> 💡 **확인 부탁**: 11번의 **컬럼 개수 분류(6+8+9+6+19)** 는 제가 5장 소제목 기준으로 나눈 것입니다. 실제 컬럼을 세어보시고 숫자가 맞는지 검증해주세요. 안 맞으면 바로 수정하겠습니다.

---